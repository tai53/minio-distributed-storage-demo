# Sao chép file này thành .env rồi điền giá trị thật (KHÔNG commit .env lên Git)

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
MINIO_BUCKET_NAME=demo-bucket
MINIO_ENDPOINT=localhost:9000
